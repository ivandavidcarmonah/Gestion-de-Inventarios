import { RolesDirective } from './roles.directive';

describe('RolesDirective', () => {
  it('should create an instance', () => {
    const directive = new RolesDirective();
    expect(directive).toBeTruthy();
  });
});
